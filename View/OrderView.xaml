<UserControl
    x:Class="Exchange_App.View.OrderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:Exchange_App.View"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tool="clr-namespace:Exchange_App.Tools"
    xmlns:uc="clr-namespace:Exchange_App.CustomUserControls"
    d:DesignHeight="621.6"
    d:DesignWidth="761.6"
    mc:Ignorable="d">
    <UserControl.Resources>
        <tool:ProductPreviewImageConverter x:Key="productPreviewImageConverter" />
        <tool:ShowTotalConverter x:Key="showTotalConverter" />
        <tool:CurrencyToStringConverter x:Key="currencyConverter" />
    </UserControl.Resources>
    <Grid Background="White">
        <Grid Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="4*" />
            </Grid.RowDefinitions>
            <Grid Background="DarkOrange">
                <StackPanel
                    Grid.Row="0"
                    Margin="10"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        Margin="0,0,10,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="AppBadge" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="White"
                        Text="My Orders" />
                    <TextBlock />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Panel.ZIndex="100">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <Grid Background="White">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="31*" />
                                <ColumnDefinition Width="743*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button
                                Command="{Binding OnSearchCommand}"
                                CommandParameter="{Binding ElementName=searchBar}"
                                Style="{StaticResource MaterialDesignToolButton}">
                                <materialDesign:PackIcon
                                    Width="18"
                                    Height="16"
                                    VerticalAlignment="Center"
                                    Kind="Search"
                                    Opacity=".56" />
                            </Button>

                            <TextBox
                                x:Name="searchBar"
                                Grid.Column="1"
                                Grid.ColumnSpan="3"
                                MinWidth="200"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                materialDesign:HintAssist.Hint="Find order"
                                materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                                BorderThickness="0">
                                <TextBox.InputBindings>
                                    <KeyBinding
                                        Key="Return"
                                        Command="{Binding OnSearchCommand}"
                                        CommandParameter="{Binding ElementName=searchBar}" />


                                </TextBox.InputBindings>

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="TextChanged">
                                        <i:InvokeCommandAction Command="{Binding OnSearchCommand}" CommandParameter="{Binding ElementName=searchBar}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>
                        </Grid>


                    </Grid>
                    <uc:UCFilter />

                </StackPanel>





                <ScrollViewer Grid.Row="2" Background="White">
                    <ItemsControl Grid.IsSharedSizeScope="True" ItemsSource="{Binding Orders}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    x:Name="Border"
                                    Padding="8"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="0,0,0,1"
                                    Cursor="Hand">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseLeftButtonDown">
                                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:OrderView}}, Path=DataContext.ShowOrderDetailsCommand}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock FontWeight="Bold" Text="{Binding Product.ProductName}" />
                                            <TextBlock Text="{Binding OrderDate}" />
                                            <TextBlock Foreground="DarkOrange" Text="{Binding Total, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource currencyConverter}}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource MaterialDesignSelection}" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Grid>

            <Grid
                Grid.Row="1"
                Panel.ZIndex="9999"
                Background="White"
                Visibility="{Binding IsShowOrderDetail}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>




                <ScrollViewer Grid.Row="1" Margin="10">
                    <Border
                        Grid.Row="1"
                        materialDesign:ElevationAssist.Elevation="Dp0"
                        BorderBrush="Gray"
                        BorderThickness=".1"
                        CornerRadius="10">
                        <StackPanel Grid.Row="1" Margin="5">

                            <Border
                                Padding="5"
                                Background="Orange"
                                CornerRadius="5">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            Margin="5"
                                            FontSize="20"
                                            FontWeight="Bold"
                                            Foreground="White">
                                            <Run Text="Status: " />
                                            <Run Text="{Binding CurrentOrder.OrderStatus}" />
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Border
                                            Width="128"
                                            Height="128"
                                            Margin="5"
                                            BorderBrush="LightGray"
                                            BorderThickness="0.5"
                                            CornerRadius="5">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding CurrentOrder.Product.Images, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource productPreviewImageConverter}}" Stretch="UniformToFill" />
                                            </Border.Background>
                                        </Border>
                                        <StackPanel Margin="5" Orientation="Vertical">
                                            <TextBlock Foreground="White">
                                                <Run Text="Product name: " />
                                                <Run FontWeight="Bold" Text="{Binding CurrentOrder.Product.ProductName}" />

                                            </TextBlock>


                                            <Separator Height="5" Visibility="Hidden" />
                                            <StackPanel Width="auto" Orientation="Horizontal">
                                                <TextBlock Foreground="White">
                                                    <Run Text="Quantity: " />
                                                </TextBlock>
                                                <Separator Width="5" Visibility="Hidden" />

                                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="40" />
                                                        <ColumnDefinition />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock
                                                        Grid.Column="1"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Foreground="White"
                                                        Text="{Binding CurrentOrder.Quantity}" />

                                                </Grid>
                                            </StackPanel>

                                            <Separator Height="5" Visibility="Hidden" />
                                            <Border Background="White" CornerRadius="10">
                                                <TextBlock Padding="10">
                                                    <Run FontWeight="Bold" Text="Price: " />
                                                    <Run
                                                        FontWeight="Bold"
                                                        Foreground="DarkOrange"
                                                        Text="{Binding CurrentOrder.Total, Converter={StaticResource currencyConverter}}" />
                                                </TextBlock>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>


                                </StackPanel>
                            </Border>

                            <Border
                                Grid.Column="1"
                                Padding="5"
                                CornerRadius="10">
                                <StackPanel Grid.Column="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock
                                            Margin="5"
                                            FontSize="20"
                                            FontWeight="UltraBold"
                                            Foreground="DarkOrange">
                                            Billing information
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Margin="10" Orientation="Vertical">
                                        <StackPanel>
                                            <TextBlock
                                                FontSize="12"
                                                FontWeight="Bold"
                                                Foreground="DarkOrange">
                                                Please choose receiving method
                                            </TextBlock>
                                            <Separator Height="5" Visibility="Hidden" />

                                            <TextBlock Text="{Binding CurrentUser.Address}" />
                                        </StackPanel>

                                        <Separator Height="5" Visibility="Hidden" />


                                    </StackPanel>
                                    <Border Margin="5">
                                        <StackPanel>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                    <RowDefinition />
                                                </Grid.RowDefinitions>
                                                <StackPanel
                                                    Grid.Row="0"
                                                    Margin="10"
                                                    Orientation="Vertical">
                                                    <StackPanel.Resources>
                                                        <Style TargetType="{x:Type Grid}">
                                                            <Setter Property="Margin" Value="0,5,0,0" />
                                                        </Style>
                                                    </StackPanel.Resources>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition />
                                                            <ColumnDefinition />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock>
                                                            Product price
                                                        </TextBlock>
                                                        <TextBlock
                                                            Grid.Column="1"
                                                            Text="{Binding CurrentOrder.Product.Sell_price, Converter={StaticResource currencyConverter}}"
                                                            TextAlignment="Right" />
                                                    </Grid>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition />
                                                            <ColumnDefinition />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock>
                                                            Shipping fee
                                                        </TextBlock>
                                                        <TextBlock Grid.Column="1" TextAlignment="Right">
                                                            0
                                                        </TextBlock>
                                                    </Grid>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition />
                                                            <ColumnDefinition />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock FontSize="15" FontWeight="Bold">
                                                            Total payment
                                                        </TextBlock>
                                                        <TextBlock
                                                            Grid.Column="1"
                                                            FontSize="15"
                                                            FontWeight="Bold"
                                                            Foreground="Orange"
                                                            Text="{Binding CurrentOrder.Total, Converter={StaticResource currencyConverter}}"
                                                            TextAlignment="Right" />
                                                    </Grid>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>

                                    </Border>

                                    <StackPanel Orientation="Vertical">
                                        <Button
                                            HorizontalAlignment="Left"
                                            materialDesign:ButtonAssist.CornerRadius="10"
                                            materialDesign:ElevationAssist.Elevation="Dp0"
                                            Background="Red"
                                            BorderBrush="Red"
                                            Command="{Binding HideCheckoutCommand}"
                                            CommandParameter="{Binding CurrentOrder.Product.ProductID}"
                                            Foreground="White">
                                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                                <TextBlock
                                                    VerticalAlignment="Center"
                                                    FontWeight="Bold"
                                                    Foreground="White">
                                                    Cancel
                                                </TextBlock>
                                                <Separator Width="5" Visibility="Hidden" />
                                                <materialDesign:PackIcon VerticalAlignment="Center" Kind="Cancel" />
                                            </StackPanel>

                                        </Button>
                                    </StackPanel>
                                </StackPanel>

                            </Border>
                        </StackPanel>

                    </Border>
                </ScrollViewer>

            </Grid>

        </Grid>
    </Grid>
</UserControl>
